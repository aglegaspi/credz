
$('#select_two').append('<%= escape_javascript options_for_select([[@artist.artist_name, @artist.id]])%>');
<%# find the element by the following ID, then insert content at the bottom of the select form supplying adding the new Artist Name and ID %>

$('#exampleModal').modal('toggle')
<%# this will toggle the modal so it will close after the code above is ran.  %>